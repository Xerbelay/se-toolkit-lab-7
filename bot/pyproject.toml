[project]
name = "lms-bot"
version = "0.1.0"
description = "Telegram bot for LMS"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.13.1",
    "pydantic-settings>=2.6.1",
]

[tool.uv]
package = false
